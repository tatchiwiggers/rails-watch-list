<div class="container">
  <%= simple_form_for (@list) do |f| %>
    <%= f.input :name, required: false %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Create List", class: "btn btn-secondary" %>
  <% end %>
</div>